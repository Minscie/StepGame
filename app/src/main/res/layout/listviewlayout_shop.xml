<?xml version="1.0" encoding="utf-8"?> <!-- This is the custom listivew layout for the shop -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/com.minscie.sami.stepgame"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">


    <ImageView
        android:id="@+id/itemImageView"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_marginStart="15dp"
        android:layout_marginTop="15dp"
        android:layout_marginBottom="15dp"
        app:srcCompat="@mipmap/ic_launcher" />

    <TextView
        android:id="@+id/itemNameTextView"
        android:layout_width="140dp"
        android:layout_height="wrap_content"
        android:text="TextView"
        android:textAppearance="@style/TextAppearance.AppCompat.Title"
        android:layout_marginStart="11dp"
        android:layout_marginEnd="11dp"
        android:layout_alignTop="@+id/itemImageView"
        android:layout_toEndOf="@+id/itemImageView" />

    <TextView
        android:id="@+id/itemDescTextView"
        android:layout_width="140dp"
        android:layout_height="35dp"
        android:text="TextView"
        android:layout_alignStart="@+id/itemNameTextView"
        android:layout_alignBottom="@+id/itemImageView" />

    <Button
        android:id="@+id/buyButton"
        style="@style/Widget.AppCompat.Button.Colored"
        android:layout_width="100dp"
        android:layout_height="60dp"
        android:text="Button"
        android:layout_alignTop="@+id/itemNameTextView"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="15dp" />
</RelativeLayout>